#include "main.h"
/**
 * _strchr - The Entry point
 * @s: Is the input
 * @c: Is the input
 * Return: Always 0 (Success)
 */
char *_strchr(char *s, char c)
{
	int i = 0;

	for (; s[i] >= '\0'; i++)
	{
		if (s[i] == c)
			return (&s[i]);
	}
	return (0);
}
