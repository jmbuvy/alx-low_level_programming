#include "main.h"

/**
 * _islower - Function checking for lowercase char
 * @c: The character to Check
 * Return: 1 if c is lowercase, 0 If not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

